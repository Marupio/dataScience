#include<PktCards.h>

// *** Member operators *** //

/*std::ostream& ds::operator<<(std::ostream& os, const PktCards& c) {
    os << first << " " << second;
    return os;
}

std::istream& ds::operator>>(std::istream& in, PktCards& c) {
    in >> first;
    in >> second;
    return in;
}*/
